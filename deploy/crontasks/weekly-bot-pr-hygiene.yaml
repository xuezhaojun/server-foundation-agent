# CronTask: Weekly Bot PR Hygiene
# Runs every Monday at 06:00 CST (Asia/Shanghai)
# Manual trigger:
#   kubectl annotate crontask weekly-bot-pr-hygiene kubeopencode.io/trigger=true -n server-foundation
apiVersion: kubeopencode.io/v1alpha1
kind: CronTask
metadata:
  name: weekly-bot-pr-hygiene
  namespace: server-foundation
  labels:
    app.kubernetes.io/name: weekly-bot-pr-hygiene
    app.kubernetes.io/part-of: server-foundation-agent
spec:
  schedule: "0 6 * * 1"
  timeZone: Asia/Shanghai
  concurrencyPolicy: Forbid
  maxRetainedTasks: 5
  taskTemplate:
    metadata:
      labels:
        app.kubernetes.io/name: weekly-bot-pr-hygiene
        app.kubernetes.io/part-of: server-foundation-agent
    spec:
      templateRef:
        name: server-foundation-template
      description: |
        Follow @workflows/weekly-bot-pr-hygiene.md to complete the task.
