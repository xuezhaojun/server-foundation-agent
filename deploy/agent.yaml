# Copyright Contributors to the KubeOpenCode project
# Unified agent using "repo as agent" pattern
# Identity is defined in .claude/CLAUDE.md, skills in .claude/skills/
apiVersion: kubeopencode.io/v1alpha1
kind: Agent
metadata:
  name: server-foundation-agent
  namespace: server-foundation
  labels:
    app.kubernetes.io/name: server-foundation-agent
    app.kubernetes.io/part-of: server-foundation-agent
spec:
  profile: |
    Server Foundation team agent for Red Hat ACM (Advanced Cluster Management) repository maintenance.
  agentImage: quay.io/kubeopencode/kubeopencode-agent-opencode:latest
  executorImage: quay.io/kubeopencode/kubeopencode-agent-devbox:latest
  workspaceDir: /workspace
  serviceAccountName: server-foundation-agent
  maxConcurrentTasks: 2
  quota:
    maxTaskStarts: 100
    windowSeconds: 86400
  config: |
    {
      "$schema": "https://opencode.ai/config.json",
      "model": "google/gemini-3.1-pro-preview",
      "small_model": "google/gemini-3-flash-preview",
      "share": "disabled",
      "autoupdate": false,
      "agent": {
        "general": {
          "model": "google/gemini-3-flash-preview"
        }
      }
    }
  contexts:
    - name: agent-repo
      description: "Agent identity, rules, and skills (repo-as-agent pattern)"
      type: Git
      git:
        repository: https://github.com/stolostron/server-foundation-agent.git
        ref: main
      mountPath: .
  credentials:
    - name: git
      secretRef:
        name: git-settings
    - name: gemini
      secretRef:
        name: gemini-credentials
    - name: github-app
      secretRef:
        name: github-bot-credentials
    - name: github-token
      secretRef:
        name: github-token
    - name: slack
      secretRef:
        name: slack-webhook-url
